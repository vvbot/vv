/**@type {import("./typings").ConfigObject} **/
module.exports = {
    // Internal Bot Related You can't See stuff
    token: "YOUR TOKEN",
    admins: [
        "YOUR USER ID"
    ],
    prefix: ">",
    debug_mode: false,

    // API Tokens
    chewey_bot: "Cheweybot API Token",
    nasa: "NASA API token",

    // Webhooks
    webhooks: {
        N: {
            id: "Webhook ID",
            token: "Webhook Token"
        }
    }
}